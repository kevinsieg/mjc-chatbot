# Copy to frontend/.env.local for `npm run dev` on the host (backend running locally or via make dev-backend-local)
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
# Public URL of this app (widget.js, page /widget-test). Required at build time.
NEXT_PUBLIC_WIDGET_BASE_URL=http://localhost:3000
